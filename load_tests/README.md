### Folders 
`/lil` -> 1 request per second | 1 read per second | 1 write per second 

`/mid` -> 1,000 requests per second | 1,000 reads per second | 1,000 writes per
second

`/big` -> 10,000 requests per second | 10,000 reads per second | 10,000 writes
per second 
